{
  "$schema": "http://json-schema.org/draft-04/hyper-schema#",
  "type": "object",
  "title": "FAOSTAT API",
  "description": "",
  "definitions": {
    "api_key": {
      "type": "string",
      "title": "API Key",
      "description": "Not available in Version 1.0."
    },
    "client_key": {
      "type": "string",
      "title": "Client Key",
      "description": "Not available in Version 1.0."
    },
    "datasource": {
      "type": "string",
      "title": "Datasource",
      "description": "Name code of the datasource.",
      "enum": [
        "faostat",
        "faostatdb",
        "faostatprod",
        "faostatproddiss"
      ]
    },
    "lang": {
      "type": "string",
      "title": "Language",
      "description": "Language of the data retrieved.",
      "enum": [
        "en",
        "fr",
        "es"
      ]
    },
    "code": {
      "type": "string",
      "title": "Code",
      "description": "Object's code."
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "Object's label."
    },
    "ord": {
      "type": "integer",
      "title": "Ord",
      "description": "Object's order."
    }
  },
  "links": [
    {
      "href": "/v1.0/{lang}/groups/",
      "method": "GET",
      "rel": "groups",
      "description": "List of available FAOSTAT domains.",
      "title": "Groups",
      "schema": {
        "type": "object",
        "name": "Groups Input",
        "description": "Input of 'groups' service.",
        "properties": {
          "datasource": {
            "$ref": "#/definitions/datasource"
          },
          "lang": {
            "$ref": "#/definitions/lang"
          }
        }
      },
      "targetSchema": {
        "type": "array",
        "title": "Groups Output",
        "description": "Output of 'groups' service.",
        "items": {
          "type": "object",
          "title": "Group",
          "description": "FAOSTAT group.",
          "properties": {
            "code": {
              "$ref": "#/definitions/code"
            },
            "label": {
              "$ref": "#/definitions/label"
            },
            "ord": {
              "$ref": "#/definitions/ord"
            }
          }
        }
      }
    }
  ]
}